{
  "name": "github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.13.0",
    "jquery": "^3.4.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router-dom": "^5.1.0",
    "react-scripts": "3.1.2",
    "rellax": "^1.12.1"
  },
  "scripts": {
    "buildcss": "postcss src/css/tailwind.src.css -o src/css/tailwind.css",
    "deploy": "gh-pages -d build -b master",
    "start": "npm run buildcss && react-scripts start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && npm run buildcss && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://jotahws.github.io/",
  "devDependencies": {
    "autoprefixer": "^9.7.6",
    "gh-pages": "^2.1.1",
    "postcss-cli": "^7.1.0",
    "tailwindcss": "^1.2.0"
  }
}
